# Optional API override for local development.
# In Hostinger production, the default /api/index.php is used.
VITE_APP_API_ENDPOINT="/api/index.php"

# Optional local proxy target used by Vite dev server.
VITE_DEV_API_PROXY_TARGET="https://carwash46.com"
